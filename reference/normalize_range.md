# Normalize values between a given range

Normalize values between a given range

## Usage

``` r
normalize_range(x, a = 0, b = 1)
```

## Arguments

- x:

  Values to normalize

- a:

  Min of range

- b:

  max of range

## Value

Normalized values
