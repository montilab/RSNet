# Convert the string of "low,avg,high" to numeric value

Convert the string of "low,avg,high" to numeric value

## Usage

``` r
.to_numeric(x)
```

## Arguments

- x:

  a string in the format of "low,avg,high"

## Value

a numeric vector
