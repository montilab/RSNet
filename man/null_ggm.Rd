% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/null_ggm.R
\name{null_ggm}
\alias{null_ggm}
\title{Differential connectivity nulls via permutation/bootstrap + GGM}
\usage{
null_ggm(
  dat,
  group_col = "phenotype",
  inference_method = c("D-S_NW_SL", "B_NW_SL"),
  shuffle_method = c("permutation", "bootstrap"),
  shuffle_iter = 100,
  balanced = FALSE,
  filter = c("pval", "fdr", "none"),
  threshold = 0.05,
  n_cores = NULL,
  seed = NULL
)
}
\arguments{
\item{dat}{data.frame; p numeric feature columns + one label column.}

\item{group_col}{character; name of the label column (default \code{"phenotype"}).}

\item{inference_method}{one of \code{c("D-S_NW_SL", "B_NW_SL")}.}

\item{shuffle_method}{character; one of \code{c("permutation","bootstrap")}.}

\item{shuffle_iter}{integer; number of null resamples (default 100).}

\item{balanced}{logical; only used when \code{shuffle_method = "permutation"}.
If \code{TRUE}, downsample to equal group sizes before permutation.}

\item{filter}{character; one of \code{c("pval","fdr","none")}.}

\item{threshold}{numeric; threshold for edge inclusion when \code{filter != "none"}.}

\item{n_cores}{integer; number of cores parallel computing}

\item{seed}{integer; random seet}
}
\value{
A list of length \code{shuffle_iter}; each element is a named list
  (by group labels) of igraph objects
}
\description{
Generates a collection of null networks by repeatedly shuffling the data
(via permutation or bootstrap) and fitting a sparse Gaussian graphical model
(SILGGM) within each label-defined group. Each iteration returns a named list
of two igraph objects (one per group). Edge attributes include:
\itemize{
  \item \code{qval} — BH-adjusted p-values
  \item \code{pval} — p-values from precision-matrix inference
  \item \code{pcor} — partial correlations
  \item \code{sign} — sign of partial correlation (+1 / -1)
}
}
