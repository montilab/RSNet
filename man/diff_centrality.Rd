% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diff_centrality.R
\name{diff_centrality}
\alias{diff_centrality}
\title{Differential centrality between two observed networks}
\usage{
diff_centrality(
  obs_networks,
  dat,
  group_col = "phenotype",
  null_networks = NULL,
  alternative = c("two.sided", "greater", "less"),
  inference_method = c("D-S_NW_SL", "B_NW_SL"),
  shuffle_method = c("permutation", "bootstrap"),
  shuffle_iter = 100,
  balanced = FALSE,
  filter = c("pval", "fdr", "none"),
  threshold = 0.05,
  n_cores = NULL,
  seed = NULL
)
}
\arguments{
\item{obs_networks}{A *named* list of length 2 containing two \pkg{igraph}
objects. The names (for example \code{c("healthy", "disease")}) must match
values present in \code{dat[[group_col]]}.}

\item{dat}{A data frame passed to \code{null_ggm()} to construct the null.}

\item{group_col}{Character scalar; name of the column in \code{dat} indicating
group labels. Default is \code{"phenotype"}.}

\item{null_networks}{A list of null networks from \code{null_ggm()}.}

\item{alternative}{Character; one of \code{"two.sided"}, \code{"greater"},
or \code{"less"} defining the alternative hypothesis.}

\item{inference_method}{Character; forwarded to \code{null_ggm()} (for example
\code{"D-S_NW_SL"} or \code{"B_NW_SL"}).}

\item{shuffle_method}{Character; one of \code{"permutation"} or
\code{"bootstrap"} specifying how the null is generated by \code{null_ggm()}.}

\item{shuffle_iter}{Integer; number of null replicates to generate.
Default is \code{100}.}

\item{balanced}{Logical; forwarded to \code{null_ggm()}.}

\item{filter}{Character; one of \code{"pval"}, \code{"fdr"}, or \code{"none"},
forwarded to \code{null_ggm()}.}

\item{threshold}{Numeric; forwarded to \code{null_ggm()}.}

\item{n_cores}{Integer or \code{NULL}; forwarded to \code{null_ggm()}.}

\item{seed}{Optional seed; forwarded to \code{null_ggm()}.}
}
\value{
A \code{data.frame} with rows corresponding to nodes (row names taken
  from the centrality matrices) and columns interleaved by statistic:
  \itemize{
    \item If \code{shuffle_method == "permutation"}: \eqn{p x (2k)} with
      columns \code{<stat>_diff}, \code{<stat>_pval}.
    \item If \code{shuffle_method == "bootstrap"}: \eqn{p x (3k)} with
      columns \code{<stat>_diff}, \code{<stat>_pval}, \code{<stat>_95pct_CI}.
      (The CI level is fixed at 95 percent in the current implementation.)
  }
}
\description{
Compute per-node, per-statistic differences in centrality between two
observed networks (reported as \code{group2 - group1}) and assess
significance using a null generated by \code{null_ggm()} via either
permutation or bootstrap.
}
