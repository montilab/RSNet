# Filter edges whose effect size is small

Filter edges whose effect size is small

## Usage

``` r
.abs_pcor_filter(x, pos_cut, neg_cut)
```

## Arguments

- x:

  the average partial correlation

- pos_cut:

  a threshold for positive partial correlation

- neg_cut:

  a threshold for negative partial correlation

## Value

the average value or 0
